
# TextNet
Bridging the digital divide by bringing Large Language Models to offline & low-coverage communities via SMS.

**TextNet** is a [Briefly describe what TextNet does, e.g., "powerful text processing library," "neural network for text analysis," or "real-time text communication platform"] designed to [Main goal, e.g., "simplify natural language tasks" or "enable seamless text networking"].

> **Note:** This project is currently under active development.

## Prerequisites
* An account with an SMS provider (e.g., Twilio).
* Gemini API Key

API Key for your preferred LLM provider.
## 🛠️ Tech Stack

* **Language**: [e.g., TypeScript / JavaScript / Python]
* **Framework**: [e.g., React / Node.js / Express]
* **Tools**: [e.g., Webpack, Jest, ESLint]

## 📦 Installation

Clone the repository and install the dependencies:

```bash
# Clone the repository
git clone https://github.com/Gxgxn/TextNet.git

# Navigate to the project directory
cd TextNet

# Install dependencies
npm install
# or
yarn install

```

## 💻 Usage

### Configuration

You can configure `TextNet` by creating a `.env` file or passing options directly:

```env
# Twilio Credentials
# Copy this file to .env and fill in your values
TWILIO_ACCOUNT_SID=your-account-sid-here
TWILIO_AUTH_TOKEN=your-auth-token-here
TWILIO_PHONE_NUMBER=+1234567890

# Google Gemini API
# Get your key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Server Configuration
PORT=8080

# Redis (use rediss:// for TLS, e.g. Upstash)
REDIS_URL= redis://localhost:6379

```

## 🤝 Contributing

Contributions are welcome! Please follow these steps to contribute:

1. **Fork** the repository.
2. Create a new **Branch** (`git checkout -b feature/AmazingFeature`).
3. **Commit** your changes (`git commit -m 'Add some AmazingFeature'`).
4. **Push** to the branch (`git push origin feature/AmazingFeature`).
5. Open a **Pull Request**.

Please ensure your code follows the project's coding standards and includes appropriate tests.

## 📝 License

Distributed under the **MIT License**. See `LICENSE` for more information.

---

*Project Link: [https://github.com/Gxgxn/TextNet*](https://github.com/Gxgxn/TextNet)
